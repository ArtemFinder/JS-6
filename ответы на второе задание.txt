if (!("a" in window)) {
    var a = 1;
}
alert(a);

Если a нету в window то переменной присвоется значение и выведется в сообщении.

var b = function a(x) {
    x && a(--x);
};
alert(a);

Не понял для чего Х. Если его нету, то b должно присвоиться значение a.

function a(x) {
    return x * 2;
}
var a;
alert(a);

В функцию не передается переменная. Не думаю что это правильно. 

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

Странная функция. Если при вызове функции, аргументу "а" передается 3, но в самой функции аргументу "а" присваивается 10, которая потом выводится в сообщении. Не понял смысл существования функции.

function a() {
    alert(this);
}
a.call(null);

this пустой и возвращает значение по умолчанию "[object Window]".
